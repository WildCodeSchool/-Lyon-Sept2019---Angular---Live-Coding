let movies = ["Kill Bill", "Volte face", "Ben hur", "Blanche neige", "Le parrain"];
//let movies = new Array();

for (let isabelle = 0; isabelle < movies.length; isabelle++) {
    console.log(movies[isabelle]);
}